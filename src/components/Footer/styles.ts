import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    footer: {
    flexDirection: 'row',
    justifyContent: 'space-around',
    alignItems: 'center',  
    flex: 0.2,
    width: 300,
}
})